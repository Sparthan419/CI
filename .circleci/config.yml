version: '2.1'
orbs:
  node: circleci/node@5.0.2
jobs:
  test:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - checkout
      - node/install:
          node-version: '14.18.1'
      - run: npm install && npm test
workflows:
  test_and_deploy:
    jobs:
      - test